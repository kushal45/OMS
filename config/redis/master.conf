bind 0.0.0.0
port 6379
protected-mode no
# requirepass yoursecuremasterpassword # Uncomment and set a password in production
# masterauth yoursecuremasterpassword # If master has a password, replicas need this
# appendonly yes # Optional: for persistence
# save "" # Disable RDB persistence if using AOF or if data loss is acceptable for cache